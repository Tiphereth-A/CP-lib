建议用于能方便求出 \(f(p^i\cdot q)\) 的简单积性函数 \(f\), 一些用法参见 \fullref{ssec:nt-lsieve-func}

\paragraph{豆知识} 用 \verb|std::vector<bool>| 优化的 Eratosthenes 筛常数更小, 可能比 Euler 筛更快

// {lib.hpp,start=3}
// {usage.cpp,start=2}
