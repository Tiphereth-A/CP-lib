本书代码默认数组下标从 $0$ 开始 ($[0, n)$), 故需注意题目下标是从 $0$ 开始 ($[0, n)$) 还是从 $1$ 开始 ($[1, n]$)

#raw(
  read("main.cpp").split("\n").slice(1).join("\n"),
  lang: "cpp",
  block: true,
)

#raw(
  read("test.cpp").split("\n").slice(1).join("\n"),
  lang: "cpp",
  block: true,
)

#raw(
  read(".clang-format"),
  lang: "yaml",
  block: true,
)

#raw(
  read("run.sh"),
  lang: "bash",
  block: true,
)
