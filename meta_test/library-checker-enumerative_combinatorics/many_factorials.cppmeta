#define PROBLEM "https://judge.yosupo.jp/problem/many_factorials"

#include "../../include/util/alias_num.hpp"

CEXP u32 MOD = 998244353;

// ---<GENTC>--- begin
// ---<GENTC>--- append factorial
// ---<GENTC>--- exclude mints-s30.factorial-fact_helper
// ---<GENTC>--- exclude mintd-d31.factorial-fact_helper
// ---<GENTC>--- exclude mints-s63.factorial-fact_helper
// ---<GENTC>--- exclude mintd-d63.factorial-fact_helper
// ---<GENTC>--- end

int main() {
  std::cin.tie(nullptr)->std::ios::sync_with_stdio(false);
  u32 t;
  std::cin >> t;
  while (t--) {
    u32 n;
    std::cin >> n;
    std::cout << fact_t::get_fact(n) << '\n';
  }
  return 0;
}
