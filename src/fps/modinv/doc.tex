对给定的多项式 \(f(x)\), \(g(x)\), 返回 \(f(x)\) 模 \(g(x)\) 的逆元, 即返回满足如下条件的多项式 \(h(x)\):

\[
    \deg(h)<\deg(g) \quad\text{and}\quad f(x)h(x)\equiv 1\pmod{g(x)}
\]

% {lib.hpp,start=4,stop=-2}
% {usage.cpp,start=2}
